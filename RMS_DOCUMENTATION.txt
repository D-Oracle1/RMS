================================================================================
 EASYLAND - COMPREHENSIVE SOFTWARE DOCUMENTATION
 Easyland - Enterprise PropTech Platform
 Generated: March 23, 2026
================================================================================

TABLE OF CONTENTS
-----------------
1.  EXECUTIVE SUMMARY
2.  ARCHITECTURE OVERVIEW
3.  TECH STACK
4.  DATABASE SCHEMA
5.  BACKEND MODULES (Implemented)
6.  FRONTEND PAGES & FEATURES (Implemented)
7.  REAL-TIME FEATURES
8.  SECURITY & AUTH
9.  DEPLOYMENT
10. WHAT NEEDS TO BE IMPLEMENTED / GAPS
11. MODULE-BY-MODULE STATUS MATRIX
12. RECENT CHANGES (2026-03-14 to 2026-03-18)

================================================================================
1. EXECUTIVE SUMMARY
================================================================================

Easyland is an enterprise-grade, multi-tenant PropTech
platform designed for real estate companies in Nigeria. It provides end-to-end
management for realtors, clients, properties, sales, commissions, HR, and more.

Key Statistics:
  - 24+ backend modules
  - 175+ API endpoints
  - 82+ frontend pages across 7 route groups
  - 5 user roles (SUPER_ADMIN, ADMIN, REALTOR, CLIENT, STAFF)
  - Multi-tenant architecture with isolated databases per company
  - Real-time features via Pusher WebSockets
  - PWA-enabled with offline support
  - Deployed on Vercel (frontend + backend serverless)
  - Event management with QR code check-in system
  - Registration confirmation emails with embedded QR codes
  - WhatsApp-level chat: voice notes, waveform player, localStorage persistence
  - CRM intelligence: engagement scoring, hot/cold lead ranking per client

================================================================================
2. ARCHITECTURE OVERVIEW
================================================================================

Multi-Tenant Architecture:
  - Master Database: Stores company/tenant records and super admin accounts
  - Tenant Database: Each company gets an isolated PostgreSQL database
  - PostgreSQL: Also used for chat messages (migrated from MongoDB for consistency)
  - Redis: Caching layer and job queue (BullMQ)

Request Flow:
  1. TenantMiddleware -> Resolve company from domain/invite code
  2. Global Pipes -> Validate DTOs, transform data
  3. Route Guards -> JWT auth, role check, permission check
  4. Route Handler -> Business logic
  5. Interceptors -> Logging, audit, response transformation
  6. Global Filter -> Error handling
  7. Response -> Standardized JSON: { success, data, meta?, timestamp }

NOTE: TransformInterceptor flattens paginated responses. If a service returns
{ data: [...], meta: {...} }, the interceptor lifts these to top-level:
{ success: true, data: [...items], meta: { totalPages, ... }, timestamp }
Frontend must read res.data and res.meta (NOT res.data.data / res.data.meta).

Monorepo Structure:
  /backend    - NestJS API (TypeScript)
  /frontend   - Next.js 16 App Router (React 19, TypeScript)
  /shared     - Shared types/configs
  /docker     - Docker Compose files (dev & prod)

================================================================================
3. TECH STACK
================================================================================

BACKEND:
  - Framework: NestJS 10 (TypeScript)
  - Database: PostgreSQL 16 (via Prisma ORM v5.22)
  - Cache: Redis 7
  - Auth: Passport.js + JWT + bcrypt
  - Queue: BullMQ (email jobs)
  - Real-time: Pusher
  - Email: Nodemailer (SMTP via Resend)
  - SMS: Twilio
  - Push: Firebase Cloud Messaging (FCM)
  - Storage: AWS S3 (images/documents), Vercel Blob (voice messages)
  - AI: OpenAI (with graceful rule-based fallbacks)
  - QR Codes: qrcode package (JWT-signed tokens)
  - API Docs: Swagger/OpenAPI
  - Error Tracking: Sentry (graceful degradation if DSN not set)
  - Log Aggregation: Logtail (graceful degradation)

FRONTEND:
  - Framework: Next.js 16 (App Router)
  - UI: React 19, Tailwind CSS, Radix UI primitives
  - State: React Query (@tanstack/react-query)
  - Charts: Recharts
  - Rich Text: TipTap Editor
  - Animations: Framer Motion
  - Toasts: Sonner
  - PDF: jsPDF + html2canvas
  - Calls: WebRTC + Pusher signaling
  - PWA: next-pwa with Workbox
  - QR Scanner: html5-qrcode (camera-based QR scanning)

================================================================================
4. DATABASE SCHEMA
================================================================================

MASTER DATABASE (master.prisma):
  - Company: id, name, slug, domain, databaseUrl, logo, primaryColor,
             inviteCode, isActive, plan, maxUsers, createdAt, updatedAt
  - SuperAdmin: id, email, password, firstName, lastName, phone, avatar

TENANT DATABASE (schema.prisma) - Per Company:

  ENUMS (29):
    UserRole, UserStatus, PropertyType, PropertyStatus, SaleStatus,
    CommissionStatus, LoyaltyTier, NotificationType, NotificationPriority,
    MessageType, DocumentType, TransactionType, TransactionStatus,
    RankingPeriod, OfferStatus, AwardType, PaymentPlan, PaymentStatus,
    StaffPosition, EmploymentType, AttendanceStatus, LeaveType, LeaveStatus,
    ReviewCycle, ReviewStatus, TaskPriority, TaskStatus, ChannelType,
    PayrollStatus, ContentType, PolicyType, AllowanceType, DeductionType,
    GalleryItemType,
    EventLocationType (physical, online),
    EventStatus (draft, published, closed),
    RegistrationStatus (pending, approved, rejected),
    FormFieldType (text, email, phone, number, textarea, select, checkbox, date)

  MODELS (43):
    Core:
      User, AdminProfile, RealtorProfile, ClientProfile, StaffProfile

    Real Estate:
      Property, Sale, Commission, Tax, Payment, Document, PriceHistory,
      Offer, Transaction

    Loyalty & Rankings:
      LoyaltyPoints, Ranking, StaffRanking, ClientRanking, MonthlyAward

    Communication:
      ChatRoom, Message, Notification,
      VoiceMessage  - 1:1 with Message; audioUrl (Vercel Blob), duration (ms),
                      waveform (Float[], 40 amplitude bars, normalised 0.10–0.95)
      CrmActivity   - logs every chat/voice interaction per client;
                      fields: id, clientId, realtorId, activityType, roomId,
                      messageId, metadata, createdAt

    Chat Performance Indexes (migration 20260322000000):
      @@index([roomId, createdAt(sort: Desc)])  -- covers hot message query
      @@index([lastMessageAt(sort: Desc)])       -- chat_rooms ordered list
      @@index([type])                            -- room type filter

    Staff & HR:
      Department, StaffPermission, Attendance, LeaveRequest,
      PerformanceReview, PayrollRecord, StaffTask, TaskComment,
      TeamChannel, ChannelMember, ChannelMessage, Mention,
      MessageReaction, SharedFile

    HR Policies:
      HRPolicy, PenaltyRecord, AllowanceConfig, DeductionConfig,
      SalaryStructure

    CMS & Content:
      CmsPage, GalleryItem

    Events (added 2026-03-17):
      Event       - id, title, slug, description, bannerUrl, locationType,
                    locationDetails, eventDate, registrationDeadline,
                    maxAttendees, status, isFeatured, createdAt, updatedAt
      EventFormField - id, eventId, label, fieldType, isRequired, orderIndex,
                       options (JSON for select/checkbox)
      EventRegistration - id, eventId, registrationCode (EVT-timestamp-hex),
                          userData (JSON), qrCodeToken (JWT), status,
                          checkedIn, checkedInAt, createdAt
      EventAnalytics - id, eventId, viewsCount, registrationsCount,
                       checkinsCount, conversionRate, updatedAt

    Security:
      AuditLog, RefreshToken, PasswordResetToken, SystemSetting,
      UserDevice

================================================================================
5. BACKEND MODULES (IMPLEMENTED)
================================================================================

----------------------------------------------------------------------
5.1 AUTH MODULE (src/modules/auth/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  POST /auth/register          - Register user (with role + invite code)
  POST /auth/login             - Login (dual: super admin + tenant user)
  POST /auth/refresh           - Refresh JWT token
  POST /auth/logout            - Logout, invalidate tokens
  POST /auth/change-password   - Change password (requires current)
  POST /auth/forgot-password   - Send password reset email
  POST /auth/reset-password    - Reset password with token
  GET  /auth/profile           - Get authenticated user profile
  PATCH /auth/profile          - Update profile (name, phone)
  POST /pusher/auth            - Authorize Pusher channel subscription
  POST /auth/2fa/setup         - Generate TOTP secret + QR
  POST /auth/2fa/enable        - Enable 2FA (verify first TOTP)
  POST /auth/2fa/disable       - Disable 2FA
  POST /auth/2fa/verify        - Verify TOTP code at login
  POST /auth/2fa/recovery-code - Use backup recovery code
  POST /auth/2fa/regenerate-backup-codes - New backup codes
  GET  /auth/oauth/google      - Google OAuth redirect
  GET  /auth/oauth/google/callback - Google OAuth callback
  GET  /auth/oauth/facebook    - Facebook OAuth redirect
  GET  /auth/oauth/facebook/callback - Facebook OAuth callback

Features:
  - Dual authentication (SUPER_ADMIN from master DB, users from tenant DB)
  - Password strength validation (8+ chars, upper, lower, number, special)
  - bcrypt hashing (12 rounds)
  - JWT access tokens (7 day expiry)
  - UUID refresh tokens (30 day expiry, stored in DB)
  - SHA256 hashed password reset tokens (1 hour expiry)
  - Invite code registration (first user becomes ADMIN)
  - Role-based profile creation on register
  - TOTP-based 2FA (RFC 6238, built-in implementation, no external lib)
  - 8 backup recovery codes per user
  - Google + Facebook OAuth (passport-google-oauth20, passport-facebook)

----------------------------------------------------------------------
5.2 USERS MODULE (src/modules/users/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET    /users        - List users (paginated, filtered)
  GET    /users/stats  - User counts by role
  GET    /users/:id    - Get user details
  PUT    /users/:id    - Update user
  PUT    /users/:id/status - Update status (ACTIVE/INACTIVE/SUSPENDED)
  DELETE /users/:id    - Delete user (SUPER_ADMIN only)

----------------------------------------------------------------------
5.3 PROPERTY MODULE (src/modules/property/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  POST   /properties            - Create property (REALTOR/ADMIN)
  GET    /properties            - List with advanced filters
  GET    /properties/listed     - Public property listings (no auth)
  GET    /properties/listed/:id - Public property detail (no auth)
  GET    /properties/stats      - Property statistics (ADMIN)
  GET    /properties/:id        - Property detail (auth required)
  PUT    /properties/:id        - Update property
  DELETE /properties/:id        - Delete property (ADMIN)
  POST   /properties/:id/offers    - Submit offer (CLIENT)
  POST   /properties/:id/documents - Add document (REALTOR/ADMIN)

Query Filters:
  page, limit, search, type, status, city, minPrice, maxPrice,
  minBedrooms, isListed, realtorId, sortBy, sortOrder

Property Types: RESIDENTIAL, COMMERCIAL, INDUSTRIAL, LAND, MIXED_USE,
                APARTMENT, CONDO, TOWNHOUSE, VILLA

----------------------------------------------------------------------
5.4 SALE MODULE (src/modules/sale/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  POST  /sales                 - Record new sale (REALTOR)
  GET   /sales                 - List sales with filters
  GET   /sales/stats           - Sales statistics (ADMIN)
  POST  /sales/:id/payments    - Record payment
  GET   /sales/:id/payments    - Payment history
  GET   /sales/:id             - Sale details
  PATCH /sales/:id/status      - Update status (ADMIN)
  PATCH /sales/:id/approve     - Approve pending sale (ADMIN)
  PATCH /sales/:id/reject      - Reject pending sale (ADMIN)

Features:
  - Automatic commission calculation based on loyalty tier
  - Automatic tax calculation
  - Payment plan support (full or installment)
  - Loyalty points awarded per sale
  - Overdue payment detection service

----------------------------------------------------------------------
5.5 REALTOR MODULE (src/modules/realtor/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET /realtors              - List realtors (ADMIN)
  GET /realtors/dashboard    - Personal dashboard (REALTOR)
  GET /realtors/leaderboard  - Public leaderboard
  GET /realtors/:id          - Realtor details (public)
  GET /realtors/:id/clients  - Realtor's clients
  GET /realtors/:id/properties - Realtor's properties
  PUT /realtors/:id          - Update profile

Dashboard Metrics:
  - Sales count, revenue, commission (filtered by period)
  - Tax paid, net earnings
  - Chart data by period (daily/weekly/monthly/yearly)
  - Loyalty tier, points, rank, achievements

----------------------------------------------------------------------
5.6 CLIENT MODULE (src/modules/client/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET  /clients                              - List clients
  GET  /clients/dashboard                    - Personal dashboard
  GET  /clients/:id                          - Client details
  GET  /clients/:id/properties               - Client's properties
  POST /clients/:id/properties/:pid/list     - List a property
  POST /clients/:id/properties/:pid/unlist   - Unlist a property
  GET  /clients/:id/offers                   - Offers on properties
  POST /clients/:id/offers/:oid/respond      - Accept/reject/counter

----------------------------------------------------------------------
5.7 COMMISSION MODULE (src/modules/commission/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET  /commissions           - List commissions (ADMIN)
  GET  /commissions/stats     - Commission statistics
  GET  /commissions/calculate - Calculate for value + tier
  GET  /commissions/:id       - Commission details
  POST /commissions/:id/pay   - Mark as paid (ADMIN)

Commission Rates by Loyalty Tier:
  BRONZE   (0-4,999 pts):    3%
  SILVER   (5,000-14,999):   3.5%
  GOLD     (15,000-49,999):  4%
  PLATINUM (50,000+):        5%

----------------------------------------------------------------------
5.8 TAX MODULE (src/modules/tax/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Features:
  - Default tax rate: 15%
  - Tax calculations per sale
  - Tax tracking and reporting
  - Quarterly/yearly tax summaries

----------------------------------------------------------------------
5.9 LOYALTY MODULE (src/modules/loyalty/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Features:
  - Points per sale: 100 (configurable)
  - Tier progression: BRONZE -> SILVER -> GOLD -> PLATINUM
  - Tier benefits: higher commission rates, badges, recognition
  - Leaderboard generation
  - Points history tracking

----------------------------------------------------------------------
5.10 RANKING MODULE (src/modules/ranking/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Services:
  - RankingService - Realtor rankings by sales
  - StaffRankingService - Staff performance rankings
  - ClientRankingService - Client loyalty rankings

Periods: WEEKLY, MONTHLY, QUARTERLY, YEARLY, ALL_TIME

----------------------------------------------------------------------
5.11 STAFF MODULE (src/modules/staff/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  POST   /staff                    - Create staff (ADMIN)
  GET    /staff                    - List staff
  GET    /staff/dashboard          - Personal dashboard (STAFF)
  GET    /staff/:id                - Get staff details
  PUT    /staff/:id                - Update staff
  DELETE /staff/:id                - Deactivate staff
  GET    /staff/:id/direct-reports - Direct reports
  GET    /staff/:id/team           - Full hierarchy
  PUT    /staff/:id/manager        - Assign manager
  GET    /staff/:id/permissions    - Get permissions
  POST   /staff/:id/permissions    - Assign permission
  DELETE /staff/:id/permissions/:pid - Remove permission

Permission Model:
  Resources: sales, properties, clients, staff, reports
  Actions: create, read, update, delete, manage (wildcard)

----------------------------------------------------------------------
5.12 HR MODULE (src/modules/hr/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Sub-modules (6):
  1. Attendance Service  - Clock in/out, tracking, reporting
  2. Leave Service       - Request/approve/reject, balance tracking
  3. Performance Service - Reviews, ratings, feedback
  4. Payroll Service     - Salary calculation, payslips
  5. Policy Service      - HR policies, penalty system
  6. Salary Config       - Grade setup, allowances, deductions

----------------------------------------------------------------------
5.13 DEPARTMENT MODULE (src/modules/department/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Features:
  - CRUD for departments
  - Department hierarchy (parent/child)
  - Department head assignment
  - Staff assignment to departments

----------------------------------------------------------------------
5.14 TASKS MODULE (src/modules/tasks/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Features:
  - Task CRUD
  - Assignment to staff
  - Priority levels: LOW, MEDIUM, HIGH, URGENT
  - Status: TODO, IN_PROGRESS, IN_REVIEW, COMPLETED, BLOCKED
  - Due date tracking
  - Task comments
  - Email notification on assignment

----------------------------------------------------------------------
5.15 NOTIFICATION MODULE (src/modules/notification/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET    /notifications              - Get user notifications
  GET    /notifications/unread-count - Unread count
  POST   /notifications/:id/read     - Mark as read
  POST   /notifications/read-all     - Mark all as read
  DELETE /notifications/:id          - Delete notification
  GET    /notifications/callout/staff - Get active staff
  POST   /notifications/callout      - Send callout
  POST   /notifications/callout/:id/respond - Respond to callout

Notification Types (20+):
  SALE, COMMISSION, PROPERTY, RANKING, LOYALTY, SYSTEM, CHAT,
  PRICE_CHANGE, LISTING, OFFER, AWARD, LEAVE_REQUEST, LEAVE_APPROVED,
  LEAVE_REJECTED, TASK_ASSIGNED, TASK_COMPLETED, REVIEW_SCHEDULED,
  REVIEW_COMPLETED, MENTION, CHANNEL_INVITE, PAYROLL_PROCESSED,
  CALLOUT, CALLOUT_RESPONSE

----------------------------------------------------------------------
5.16 CHAT MODULE (src/modules/chat/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED (WhatsApp-level messaging)

Services:
  ChatService   - Room CRUD, message send/fetch, read receipts, typing broadcast
  VoiceService  - Audio upload to Vercel Blob, waveform generation (40-bar sampling)
  CrmService    - Activity logging, engagement scoring, lead classification

Endpoints — Messaging:
  GET    /chat/rooms                         - Get user's chat rooms (ordered by lastMessageAt)
  POST   /chat/rooms                         - Create room (DIRECT or GROUP)
  GET    /chat/rooms/:id                     - Room details
  GET    /chat/rooms/:id/messages            - Message history (cursor-based pagination)
    Query: limit (default 30), cursor (ISO timestamp of oldest loaded message)
    Returns: messages ordered oldest→newest (reversed from DB fetch)
  POST   /chat/rooms/:id/messages            - Send text/file/image message
  POST   /chat/rooms/:id/voice              - Send voice message (multipart/form-data)
    Fields: audio (Blob), duration (ms)
    Uploads audio to Vercel Blob, generates 40-bar waveform, creates VOICE message
  POST   /chat/rooms/:id/read               - Mark room as read (unreadCount → 0)
  POST   /chat/rooms/:id/typing             - Broadcast typing indicator via Pusher
  POST   /chat/rooms/:id/participants       - Add participants
  DELETE /chat/rooms/:id/participants/:pid  - Remove participant
  DELETE /chat/rooms/:id                    - Delete room

Endpoints — CRM Intelligence (ADMIN/REALTOR only):
  GET    /chat/crm/hot-leads                - Clients with score ≥ 50 (most engaged)
  GET    /chat/crm/cold-leads               - Clients with score ≤ 20 (least engaged)
  GET    /chat/crm/follow-ups               - Clients with no activity in 7+ days
  GET    /chat/crm/client/:clientId         - Full client CRM profile + engagement score
  GET    /chat/crm/client/:clientId/activities - Paginated activity history

Message Types: TEXT, IMAGE, FILE, PROPERTY, SYSTEM, VOICE
Database: PostgreSQL (via Prisma — consolidated from MongoDB)

Performance Optimisations (added 2026-03-22):
  - Composite index (roomId, createdAt DESC) — message query uses single index scan
  - Membership check separated from message fetch (fast COUNT vs JOIN-heavy query)
  - Cursor-based pagination (no offset, consistent performance at any depth)

Voice Messages:
  - Recorded in browser via MediaRecorder API (WebM/Ogg/M4A, codec=opus preferred)
  - Uploaded to Vercel Blob storage (BLOB_READ_WRITE_TOKEN env var required)
  - Waveform: 40 amplitude bars, byte-sampled from audio buffer, normalised 0.10–0.95
  - Frontend: AudioPlayer renders waveform, highlights bars as audio plays, click-to-seek

CRM Intelligence:
  - Every text and voice message auto-logs a CrmActivity record
  - Engagement score = min(100, recentActivityCount × 5) — computed from last 30 days
  - Score 50+: hot lead | Score 21–49: warm | Score ≤ 20: cold
  - Follow-up list: clients whose last CrmActivity is older than 7 days
  - Admin CRM dashboard at /dashboard/admin/crm (tabs: Hot Leads, Follow-ups, Cold Leads)

----------------------------------------------------------------------
5.17 CMS MODULE (src/modules/cms/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Public Endpoints (no auth):
  GET /cms/public            - All public content
  GET /cms/public/:key       - Get section by key
  GET /cms/public/pages      - Published pages
  GET /cms/public/pages/:slug - Page by slug

Admin Endpoints:
  GET    /cms/sections       - All sections
  GET    /cms/sections/:key  - Get section
  PUT    /cms/sections/:key  - Create/update section
  GET    /cms/pages          - All pages
  POST   /cms/pages          - Create page
  PUT    /cms/pages/:id      - Update page
  DELETE /cms/pages/:id      - Delete page
  PATCH  /cms/pages/:id/publish   - Publish page
  PATCH  /cms/pages/:id/unpublish - Unpublish page

Content Types: PAGE, BLOG_POST, ANNOUNCEMENT, FAQ

----------------------------------------------------------------------
5.18 GALLERY MODULE (src/modules/gallery/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Public:
  GET /gallery/public - Published gallery items

Admin:
  GET    /gallery                   - All items
  POST   /gallery                   - Create item
  PUT    /gallery/reorder           - Bulk reorder
  PUT    /gallery/:id               - Update item
  PATCH  /gallery/:id/toggle-publish - Toggle publish
  DELETE /gallery/:id               - Delete item

----------------------------------------------------------------------
5.19 UPLOAD MODULE (src/modules/upload/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  POST   /upload/avatar          - Upload avatar (5MB, images)
  DELETE /upload/avatar          - Delete avatar
  POST   /upload/property-images - Upload property images (10 files, 10MB each)
  POST   /upload/gallery-images  - Upload gallery images (20 files, 10MB each)
  POST   /upload/gallery-videos  - Upload gallery videos (5 files, 100MB each)
  POST   /upload/cms-images      - Upload CMS images (5 files, 10MB each)

Storage: AWS S3
CDN: Images piped through CdnService (sharp optimization, WebP conversion)
  - Avatars/logos: max 400x400px, quality 85
  - Properties/gallery/cms: max 1920x1080px, quality 80
  - Receipts/docs: no optimization (fidelity preserved)
  - CDN URL rewriting when CDN_BASE_URL env var is set

----------------------------------------------------------------------
5.20 ADMIN MODULE (src/modules/admin/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET /admin/dashboard    - Dashboard stats by period
  GET /admin/realtors     - Realtor monitoring with filters
  GET /admin/realtors/:id - Realtor drill-down
  GET /admin/sales-feed   - Recent sales
  GET /admin/analytics    - Performance analytics

----------------------------------------------------------------------
5.21 ANALYTICS MODULE (src/modules/analytics/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET /analytics/sales         - Sales analytics
  GET /analytics/properties    - Property analytics
  GET /analytics/realtors      - Realtor analytics
  GET /analytics/commissions   - Commission analytics
  GET /analytics/market-trends - Market trends

----------------------------------------------------------------------
5.22 COMPANY MODULE (src/modules/company/) - Multi-Tenant
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Public:
  GET /companies/resolve - Resolve company from domain or invite code

SUPER_ADMIN only:
  GET   /companies           - List all companies (paginated)
  GET   /companies/overview  - Aggregated stats across all companies
  GET   /companies/:id       - Company details with stats
  POST  /companies           - Create company (provisions dedicated DB)
  PUT   /companies/:id       - Update company
  PATCH /companies/:id/toggle-active  - Toggle active status
  POST  /companies/:id/regenerate-invite - New invite code

----------------------------------------------------------------------
5.23 CRON MODULE (src/modules/cron/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED + CONFIGURED

HTTP-triggered (for Vercel serverless):
  POST /cron/rankings/daily    - Update daily rankings
  POST /cron/rankings/monthly  - Update monthly rankings
  POST /cron/rankings/yearly   - Update yearly rankings
  POST /cron/overdue-payments  - Check overdue payments

Guard: CronSecretGuard (validates x-cron-secret header)
Scheduler: Configured in backend/vercel.json (4 Vercel Cron entries)

----------------------------------------------------------------------
5.24 AUDIT MODULE (src/modules/audit/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET /audit - Query audit logs (ADMIN/SUPER_ADMIN)

Query: userId, entity, action, startDate, endDate, page, limit

----------------------------------------------------------------------
5.25 SEARCH MODULE (src/modules/search/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET /search?q=keyword&limit=10 - Global search

Searches: properties, users (admin only), clients, realtors, staff, sales
Role-scoped: results filtered based on caller's role

----------------------------------------------------------------------
5.26 EXPORT MODULE (src/modules/export/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  GET /export/sales      - Export sales data
  GET /export/tax        - Export tax data
  GET /export/payroll    - Export payroll data
  GET /export/audit      - Export audit logs
  GET /export/commission - Export commission data

Formats: CSV + XLSX (via exceljs)
Query: format=csv|xlsx, date filters

----------------------------------------------------------------------
5.27 CONTACT MODULE (src/modules/contact/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED

Endpoints:
  POST /contact - Submit contact form

Rate limit: 3 req/min
Sends: notification email to admin + auto-reply to sender

----------------------------------------------------------------------
5.28 EVENT MODULE (src/modules/event/)
----------------------------------------------------------------------
Status: FULLY IMPLEMENTED (added 2026-03-17)

Services:
  EventService        - CRUD for events
  RegistrationService - Public sign-up + QR check-in
  QrService           - JWT-signed QR token generation and verification
  EventAnalyticsService - View tracking, metrics (cached 120s)

Public Endpoints (no auth):
  GET  /events/homepage                    - Featured/upcoming events for landing page
  GET  /events/public                      - Published events list
  GET  /events/public/:slug                - Event detail by slug (increments view count)
  POST /events/public/:eventId/register    - Register for an event
    Rate limit: 5 req/60s
    Body: { userData: Record<string, unknown> }
    Returns: { registrationCode, status, qrCode (base64 PNG), createdAt }

Admin Endpoints (ADMIN, HR, STAFF, GENERAL_OVERSEER):
  POST  /events                                     - Create event
  GET   /events                                     - List events (paginated, filtered)
  GET   /events/:id                                 - Event details
  PUT   /events/:id                                 - Update event
  DELETE /events/:id                                - Delete event + all data
  PATCH /events/:id/status                          - Change status (draft/published/closed)
  GET   /events/:id/analytics                       - Event analytics (views, regs, check-ins)
  GET   /events/:id/registrations                   - List registrations (paginated, status filter)
  PATCH /events/registrations/:id/status            - Approve/reject a registration
  POST  /events/checkin                             - Check-in by QR token OR registration code
    Rate limit: 120 req/60s
    Accepts: JWT QR token from camera scan OR EVT-... registration code (manual entry)
    Returns: { alreadyCheckedIn, checkedInAt, registrationCode, eventTitle, message }

QR Code System:
  - Each registration gets a unique JWT-signed token (10-year expiry)
  - QR code is a 300x300px PNG rendered as a base64 data URL
  - Check-in accepts both camera-scanned QR token and typed registration code
  - DB lookup is the authoritative validation (JWT signature not re-verified at check-in)
  - Registration codes format: EVT-{timestamp}-{8-char-hex} (e.g. EVT-1747123456789-A1B2C3D4)

Registration Email:
  - Confirmation email sent fire-and-forget immediately after registration
  - Contains: event title, date, format, location, registration code, form data
  - QR code embedded as inline CID attachment (works in Gmail, Outlook, Apple Mail)
  - Uses registrant's email from any form field with fieldType === 'email'

Form Builder:
  - Admin defines custom form fields per event (text, email, phone, number,
    textarea, select, checkbox, date)
  - Required/optional toggle per field
  - Order index for drag-drop reordering
  - Validation: email format, phone format, required fields
  - Deduplication: prevents re-registration by email per event

Analytics (EventAnalytics):
  - viewsCount: incremented on each public event detail page load
  - registrationsCount: count of non-rejected registrations
  - checkinsCount: count of checked-in registrations
  - conversionRate: registrations / views * 100
  - Cached for 120 seconds, recomputed on each registration/check-in

----------------------------------------------------------------------
5.29 OTHER MODULES
----------------------------------------------------------------------
Award Module   - Achievement/award system           - IMPLEMENTED
AI Module      - OpenAI integration                 - IMPLEMENTED
  Endpoints: generate-description, market-price-insights,
             score-leads, sales-trend-prediction
  Fallback: Rule-based responses if OPENAI_API_KEY not set
Call Module    - WebRTC call management              - IMPLEMENTED
Device Module  - Push notification device tokens     - IMPLEMENTED
Settings Module - System-wide settings + notification preferences - IMPLEMENTED
Health Module  - Health check endpoint               - IMPLEMENTED

----------------------------------------------------------------------
5.30 COMMON INFRASTRUCTURE
----------------------------------------------------------------------

Decorators (6):
  @Public()         - Bypass JWT authentication
  @Roles(...)       - Require specific roles
  @CurrentUser()    - Inject authenticated user
  @Permissions(...) - Require staff permissions
  @NoAudit()        - Skip audit logging
  @ApiPaginatedResponse() - Swagger pagination docs

Guards (4):
  JwtAuthGuard         - JWT validation, respects @Public()
  RolesGuard           - Role-based access, SUPER_ADMIN bypasses all
  StaffPermissionGuard - Permission-based access for staff
  CronSecretGuard      - Validates x-cron-secret header

Interceptors (3):
  LoggingInterceptor    - JSON-structured HTTP request logs; skips /health endpoint
                          so keepalive pings add zero log overhead
  TransformInterceptor  - Standardize { success, data, meta?, timestamp }
  AuditInterceptor      - Log write operations with field redaction

Services (15):
  AuditService             - Fire-and-forget audit logging
  CacheService             - Redis caching with graceful fallback
  MailService              - Email delivery with 15 templates
  PushNotificationService  - Firebase push notifications
  SmsService               - Twilio SMS delivery
  PusherService            - WebSocket events
  QueueService             - BullMQ email job queue
  CdnService               - Image optimization (sharp + WebP) + URL rewriting
  SentryService            - Error tracking (graceful degradation)
  LoggerService            - Logtail structured log aggregation
  OpenAIService            - AI features (graceful degradation)
  VoiceService             - Audio upload to Vercel Blob + 40-bar waveform generation
  CrmService               - CRM activity logging + engagement scoring + lead queries

Email Templates (15):
  Password Reset, Welcome, Email Verification,
  Sale Confirmation (Realtor), Sale Confirmation (Client),
  Commission Paid, Payment Reminder,
  Leave Approved, Leave Rejected,
  Task Assigned, Performance Review,
  Purchase Payment Notification,
  Contact Form Notification, Contact Auto-Reply,
  Event Registration Confirmation (with inline QR code)

================================================================================
6. FRONTEND PAGES & FEATURES (IMPLEMENTED)
================================================================================

----------------------------------------------------------------------
6.1 PUBLIC PAGES (9 pages)
----------------------------------------------------------------------

/                  - Homepage (hero, properties, agents, features, CTA,
                     Upcoming Events section with live API data)
/about             - Company story (CMS-driven)
/features          - Platform features (CMS-driven)
/contact           - Contact form + info card (connected to backend)
/gallery           - Photo/video gallery with lightbox
/properties        - Property listing with search & filters
/properties/[id]   - Property detail with image carousel
/event/[slug]      - Public event detail (register, QR code on success)
/~offline          - PWA offline fallback

Data: All use real API.

Homepage Events Section:
  - Calls GET /events/homepage (parallel with CMS fetch)
  - Shows featured events first, falls back to upcoming events
  - Displays date, location type, registration deadline, capacity
  - "Register Now" links to /event/[slug]

----------------------------------------------------------------------
6.2 AUTH PAGES (4 pages)
----------------------------------------------------------------------

/auth/login           - Email/password login with demo credentials
/auth/register        - 3-step registration (account, personal, role)
/auth/forgot-password - Password recovery email
/auth/reset-password  - Reset password with token

Features:
  - Dual auth (super admin + tenant user)
  - OAuth buttons (Google, Facebook) - UI present, backend implemented
  - Nigerian-specific: phone format, NIN, 37 states
  - Role selection: CLIENT or REALTOR
  - Password strength requirements
  - Session storage (tab-isolated)

----------------------------------------------------------------------
6.3 ADMIN DASHBOARD (30 pages)
----------------------------------------------------------------------

/dashboard/admin                           - Overview (revenue, sales, charts)
/dashboard/admin/realtors                  - Realtor CRUD + monitoring
/dashboard/admin/clients                   - Client CRUD
/dashboard/admin/properties                - Property CRUD + image upload
/dashboard/admin/sales                     - Sales tracking, approve/reject
/dashboard/admin/commission                - Commission distribution + payouts
/dashboard/admin/tax                       - Tax reports + export
/dashboard/admin/rankings                  - Leaderboard (realtors, staff, clients)
/dashboard/admin/analytics                 - Advanced charts + analytics
/dashboard/admin/staff                     - Employee management
/dashboard/admin/departments               - Department CRUD
/dashboard/admin/hr                        - HR dashboard overview
/dashboard/admin/hr/attendance             - Clock in/out tracking
/dashboard/admin/hr/leave                  - Leave approve/reject
/dashboard/admin/hr/payroll                - Payroll processing
/dashboard/admin/hr/performance            - Performance reviews
/dashboard/admin/hr/policies               - HR policy management
/dashboard/admin/hr/salary-config          - Salary structure config
/dashboard/admin/hr/tasks                  - HR task management
/dashboard/admin/cms                       - Content management (hero, about, etc.)
/dashboard/admin/gallery                   - Media upload + management
/dashboard/admin/audit                     - Audit log viewer + export
/dashboard/admin/chat                      - Real-time messaging (voice notes, waveform player)
/dashboard/admin/crm                       - CRM intelligence dashboard (hot/cold/follow-up leads)
/dashboard/admin/notifications             - Notification management
/dashboard/admin/settings                  - Profile + system settings
/dashboard/admin/events                    - Event list (search, filter, publish/close/delete)
/dashboard/admin/events/create             - Event creation (details + form builder)
/dashboard/admin/events/[id]               - Event edit + preview
/dashboard/admin/events/[id]/registrations - Registrations list (approve/reject, QR preview, CSV export)
/dashboard/admin/events/[id]/checkin       - QR scanner + manual check-in + live analytics

Data: ALL pages use real API integration.

CRM Dashboard (/dashboard/admin/crm):
  - Three tabs: Hot Leads (score ≥ 50) / Follow-ups (idle 7+ days) / Cold Leads (score ≤ 20)
  - Stats row: lead count + average engagement score per tab
  - LeadCard component: client name, score bar (color-coded), last activity timestamp
  - Score bar: green (≥ 50), yellow (21–49), red (≤ 20)
  - Sidebar: "CRM" link added to Sales & Finance group (Target icon)

Events Admin Features:
  - List with search, status filter, pagination
  - Quick publish/close toggle on list
  - One-click navigation to registrations and check-in scanner
  - Form builder with drag-drop field ordering
  - Live attendance stats on check-in page (auto-refreshes every 10s)
  - Camera-based QR scanner (html5-qrcode)
  - Manual registration code entry fallback
  - Recent check-ins log with duplicate detection
  - CSV export of all registrations with custom form data columns

----------------------------------------------------------------------
6.4 REALTOR DASHBOARD (10 pages)
----------------------------------------------------------------------

/dashboard/realtor                - Personal stats, tier, charts
/dashboard/realtor/sales          - My sales history
/dashboard/realtor/properties     - My listings (CRUD)
/dashboard/realtor/clients        - My clients
/dashboard/realtor/commission     - Commission tracking
/dashboard/realtor/loyalty        - Points, tier, achievements
/dashboard/realtor/chat           - Messaging
/dashboard/realtor/notifications  - Personal notifications
/dashboard/realtor/settings       - Profile settings

Data: ALL pages use real API integration.

----------------------------------------------------------------------
6.5 CLIENT DASHBOARD (8 pages)
----------------------------------------------------------------------

/dashboard/client                - Portfolio summary, appreciation
/dashboard/client/properties     - Owned properties
/dashboard/client/offers         - Purchase offers (accept/reject/counter)
/dashboard/client/documents      - Property documents
/dashboard/client/chat           - Chat with realtor
/dashboard/client/notifications  - Personal notifications
/dashboard/client/settings       - Profile settings

Data: ALL pages use real API integration.

----------------------------------------------------------------------
6.6 STAFF DASHBOARD (12 pages)
----------------------------------------------------------------------

/dashboard/staff                - Clock in/out, task summary
/dashboard/staff/tasks          - Task management (Kanban-style)
/dashboard/staff/attendance     - Attendance history
/dashboard/staff/leave          - Leave requests + balance
/dashboard/staff/team           - Team members + org chart
/dashboard/staff/channels       - Department channels
/dashboard/staff/files          - Shared file storage
/dashboard/staff/reviews        - Performance reviews
/dashboard/staff/payslips       - Salary + payslip download
/dashboard/staff/chat           - Team messaging
/dashboard/staff/notifications  - Personal notifications

Data: ALL pages use real API integration.

----------------------------------------------------------------------
6.7 SUPER-ADMIN DASHBOARD (4 pages)
----------------------------------------------------------------------

/dashboard/super-admin              - Platform overview stats
/dashboard/super-admin/companies    - Company CRUD + activate/deactivate
/dashboard/super-admin/analytics    - Cross-company analytics
/dashboard/super-admin/settings     - Platform-wide settings

Data: ALL pages use real API integration.

----------------------------------------------------------------------
6.8 SHARED COMPONENTS
----------------------------------------------------------------------

Layout:
  Sidebar      - Role-based nav, collapsible, mobile drawer
  Header       - Breadcrumb, search, theme toggle, notification bell, profile
  PublicNavbar  - Public site navigation
  PublicFooter  - Footer with CMS content

Feature Components:
  ChatPage           - Full chat interface (rooms + messages + voice notes)
  VoiceRecorder      - Click-to-start/stop voice recording via MediaRecorder API;
                       live duration counter (M:SS); cancel (X) and send buttons;
                       MIME detection (prefers audio/webm;codecs=opus)
  AudioPlayer        - 40-bar waveform visualization; bars highlight as audio plays;
                       click-to-seek on waveform; play/pause toggle; time display
  IncomingCallModal  - WebRTC incoming call UI
  ActiveCallScreen   - WebRTC active call with controls
  CalloutAlertModal  - Emergency callout notification
  CalloutButton      - Admin emergency broadcast button
  AwardBanner        - Monthly award celebration
  CelebrationModal   - Award details with confetti
  Receipt/ReceiptModal - Sales receipt with PDF export
  SaleApprovalModal  - High-priority sale approval
  GalleryLightbox    - Fullscreen image/video viewer
  AreaUnitSelect     - Area unit conversion (sqm, acre, hectare, plot)
  RichTextEditor     - TipTap-based HTML editor

UI Library (17 components, Radix UI + Tailwind):
  Avatar, Badge, Button, Card, Dialog, DropdownMenu, Input, Label,
  Progress, ScrollArea, Select, Switch, Table, Tabs, Textarea, Sonner

----------------------------------------------------------------------
6.9 CONTEXTS (Real-time State)
----------------------------------------------------------------------

PusherContext:
  - WebSocket connection management
  - Channel subscriptions (user, role, presence)
  - Online user tracking
  - Auto-reconnect

NotificationContext:
  - Push notification management
  - Unread count tracking
  - Sale approval modals (high-priority)
  - Callout alerts with audio
  - Mark read / delete

ChatContext:
  - Chat room management
  - Cursor-based message pagination (30 msgs per fetch)
  - Typing indicators (broadcast + auto-expire after 3s)
  - Real-time message delivery via Pusher user event
  - Voice message send (sendVoiceMessage → multipart POST)
  - localStorage persistence (chat-storage.ts):
      Hydrates rooms + recent messages instantly on mount (zero network latency)
      Eagerly background-refreshes rooms after hydration
      Persists after every send/receive/fetch event
      Schema-versioned (SCHEMA_VERSION=2), max 20 rooms × 30 msgs per room
  - 30 s cache TTL: switching rooms within the window skips network refetch
  - roomsRef: always-current ref prevents stale closure bugs in callbacks
  - subscribeToTyping() helper: extracted to avoid code duplication

AuthenticatedProviders (authenticated-providers.tsx):
  - KeepAlive component: pings GET /health every 4 min (first ping at 60 s)
    Prevents Vercel serverless cold starts (cold boots cost 2–5 s per idle user)
    /health is excluded from LoggingInterceptor so pings have zero log overhead

CallContext:
  - WebRTC audio/video calls
  - Call initiation, accept, reject, end
  - Mute/unmute, camera toggle
  - Call duration timer
  - ICE candidate negotiation via Pusher

================================================================================
7. REAL-TIME FEATURES
================================================================================

Pusher WebSocket Channels:
  private-user-{userId}      - Personal events (notifications, calls, chat)
  private-role-{role}        - Role-based broadcasts
  presence-online            - Online user tracking
  presence-chat-{roomId}     - Chat room presence
  private-dashboard-{type}   - Dashboard metric updates
  private-property-{id}      - Property change events
  public-broadcast           - System-wide messages

Audio Notifications:
  - Regular: 2-tone chime (880Hz + 1174.66Hz)
  - Callout: Urgent alarm pattern (5 tones)
  - Response: Confirmation melody (3 ascending tones)
  - Implemented via Web Audio API

WebRTC Calls:
  - Audio and video calls between users
  - ICE servers: Google STUN servers
  - Signaling via Pusher events
  - Media controls: mute, camera toggle
  - Call duration tracking

================================================================================
8. SECURITY & AUTH
================================================================================

Authentication:
  - JWT Bearer tokens (7-day access, 30-day refresh)
  - bcrypt password hashing (12 rounds)
  - Session storage (tab-isolated)
  - Refresh token rotation
  - Password reset via email (SHA256 tokens, 1hr expiry)
  - TOTP 2FA (RFC 6238, optional per user, 8 backup recovery codes)
  - Google + Facebook OAuth (Passport strategies)

Authorization:
  - 5 roles: SUPER_ADMIN, ADMIN, REALTOR, CLIENT, STAFF
  - SUPER_ADMIN bypasses all role checks
  - Permission-based access for STAFF (resource + action matrix)
  - Route guards on every protected endpoint

Security Features:
  - Helmet.js (security headers)
  - CORS whitelist (localhost + Vercel previews)
  - Rate limiting (global: 100 req/60s; per-endpoint: login 5/min, register 3/min,
    forgot-password 3/min, contact 3/min, event registration 5/min, check-in 120/min)
  - Input validation (class-validator, whitelist mode)
  - Audit logging (all write operations)
  - Sensitive field redaction in audit logs
  - Encryption key for sensitive data
  - Sentry error tracking (5xx errors reported)
  - HTML/script injection sanitization on event registration userData

================================================================================
9. DEPLOYMENT
================================================================================

Current Deployment:
  Frontend: Vercel (Next.js serverless)
  Backend: Vercel (NestJS serverless via Express adapter)
  Database: Supabase PostgreSQL (all data including chat)
  Cache: Redis (external)
  Voice Storage: Vercel Blob (audio files for voice messages)
  Email: Resend (via Nodemailer SMTP adapter, SMTP_HOST=smtp.resend.com)

Performance Notes:
  - Gzip compression is enabled unconditionally (Vercel CDN does NOT compress
    serverless function responses automatically — removing the guard cuts JSON
    payload sizes by 60–80%)
  - KeepAlive ping every 4 min prevents serverless cold starts (2–5 s stall)
  - LoggingInterceptor skips /health to keep keepalive pings zero-overhead

Also Configured (alternative):
  Docker: docker-compose.yml (prod) + docker-compose.dev.yml
  Railway: railway.json + nixpacks.toml
  Render: render.yaml (backend + frontend services)

CI/CD:
  GitHub Actions:
    - ci.yml: Lint + test on push/PR to main/develop
    - security.yml: Dependency audit (weekly + on push)

Cron Jobs (Vercel):
  Configured in backend/vercel.json:
    - Daily rankings   (0 0 * * *)
    - Monthly rankings (0 0 1 * *)
    - Yearly rankings  (0 0 1 1 *)
    - Overdue payments (0 6 * * *)

Database Backups:
  - backend/scripts/backup.sh  (pg_dump → gzip → S3, optional encryption)
  - backend/scripts/restore.sh (download from S3, decrypt, restore)
  - Retention: configurable via BACKUP_RETENTION_DAYS (default 30 days)
  - Required env: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, S3_BACKUP_BUCKET

Environment Variables (65+):
  Database: DATABASE_URL, MASTER_DATABASE_URL, DIRECT_URL, MONGODB_URI
  Cache: REDIS_HOST, REDIS_PORT, REDIS_URL
  Auth: JWT_SECRET, JWT_EXPIRES_IN, JWT_REFRESH_SECRET, ENCRYPTION_KEY
  2FA: (uses JWT_SECRET for TOTP secret encryption)
  OAuth: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET,
         FACEBOOK_APP_ID, FACEBOOK_APP_SECRET
  Email: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_FROM
  SMS: TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER
  Push: FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY
  Storage: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_S3_BUCKET, AWS_REGION
  Voice Storage: BLOB_READ_WRITE_TOKEN (Vercel Blob — required for voice messages)
  CDN: CDN_BASE_URL (optional, rewrites blob URLs to CDN origin)
  AI: OPENAI_API_KEY (optional, falls back to rule-based)
  Monitoring: SENTRY_DSN (optional), LOGTAIL_TOKEN (optional)
  Loyalty: POINTS_PER_SALE, BRONZE/SILVER/GOLD/PLATINUM_THRESHOLD
  Commission: DEFAULT/BRONZE/SILVER/GOLD/PLATINUM_COMMISSION_RATE
  Tax: DEFAULT_TAX_RATE
  Frontend: NEXT_PUBLIC_API_URL, NEXT_PUBLIC_PUSHER_KEY, NEXT_PUBLIC_PUSHER_CLUSTER

================================================================================
10. WHAT NEEDS TO BE IMPLEMENTED / GAPS
================================================================================

----------------------------------------------------------------------
10.1 REMAINING GAPS
----------------------------------------------------------------------

1. EMAIL VERIFICATION FLOW
   Status: PARTIALLY IMPLEMENTED
   Issue: User has `emailVerified` field and email template exists,
          but full end-to-end verification flow (send OTP on register,
          enforce verified check on login) needs verification.
   Action: Test GET /auth/verify-email?otp=xxx works end-to-end.

2. TERMS & PRIVACY PAGES
   Status: NOT IMPLEMENTED
   Issue: Registration links to /terms and /privacy but pages don't exist.
   Action: Create pages or link to CMS-managed content.

3. THEME/BRANDING PER TENANT
   Status: PARTIALLY IMPLEMENTED
   Issue: CMS has branding editor (logo, colors) but primaryColor isn't
          fully applied as a CSS custom property across all pages.
   Action: Apply company primaryColor as CSS variable at the layout level.

4. OAUTH BUTTONS (FRONTEND)
   Status: UI PRESENT, BACKEND DONE
   Issue: Google/Facebook OAuth strategies exist in the backend but the
          frontend OAuth redirect flow is not yet wired up.
   Action: Implement redirect to /auth/oauth/google and /auth/oauth/facebook
           from the login page OAuth buttons.

5. TEST COVERAGE
   Status: PARTIAL (unit tests for key services only)
   Action: Expand integration tests, add E2E tests for critical paths
           (registration, check-in, sales flow, commission calculation).

----------------------------------------------------------------------
10.2 KNOWN BEHAVIOURS (NOT BUGS)
----------------------------------------------------------------------

- AbortError: The play() request was interrupted by a call to pause()
  Source: html5-qrcode scanner when stopped mid-initialization.
  Impact: Console warning only, no functional impact.

- TransformInterceptor response flattening:
  Paginated service responses { data: [], meta: {} } are lifted to top-level
  by TransformInterceptor. Frontend must read res.data and res.meta (not
  res.data.data / res.data.meta).

================================================================================
11. MODULE-BY-MODULE STATUS MATRIX
================================================================================

Module                    Backend    Frontend   Real API   Status
========================  =========  =========  =========  =================
Auth (Login/Register)     DONE       DONE       YES        PRODUCTION READY
Auth (Password Reset)     DONE       DONE       YES        PRODUCTION READY
Auth (OAuth Google/FB)    DONE       UI ONLY    PARTIAL    FRONTEND WIRING NEEDED
Auth (2FA)                DONE       DONE       YES        PRODUCTION READY
User Management           DONE       DONE       YES        PRODUCTION READY
Property CRUD             DONE       DONE       YES        PRODUCTION READY
Property Listings         DONE       DONE       YES        PRODUCTION READY
Sales Management          DONE       DONE       YES        PRODUCTION READY
Commission System         DONE       DONE       YES        PRODUCTION READY
Tax Reporting             DONE       DONE       YES        PRODUCTION READY
Loyalty Program           DONE       DONE       YES        PRODUCTION READY
Rankings/Leaderboard      DONE       DONE       YES        PRODUCTION READY
Staff Management          DONE       DONE       YES        PRODUCTION READY
HR - Attendance           DONE       DONE       YES        PRODUCTION READY
HR - Leave                DONE       DONE       YES        PRODUCTION READY
HR - Payroll              DONE       DONE       YES        PRODUCTION READY
HR - Performance          DONE       DONE       YES        PRODUCTION READY
HR - Policies             DONE       DONE       YES        PRODUCTION READY
HR - Salary Config        DONE       DONE       YES        PRODUCTION READY
HR - Tasks                DONE       DONE       YES        PRODUCTION READY
Department Mgmt           DONE       DONE       YES        PRODUCTION READY
Notifications             DONE       DONE       YES        PRODUCTION READY
Chat (Real-time)          DONE       DONE       YES        PRODUCTION READY
Chat Voice Messages       DONE       DONE       YES        PRODUCTION READY
Chat CRM Intelligence     DONE       DONE       YES        PRODUCTION READY
Calls (WebRTC)            DONE       DONE       YES        PRODUCTION READY
Callout System            DONE       DONE       YES        PRODUCTION READY
CMS (Content Mgmt)        DONE       DONE       YES        PRODUCTION READY
Gallery                   DONE       DONE       YES        PRODUCTION READY
File Upload + CDN         DONE       DONE       YES        PRODUCTION READY
Awards System             DONE       DONE       YES        PRODUCTION READY
Admin Dashboard           DONE       DONE       YES        PRODUCTION READY
Admin Analytics           DONE       DONE       YES        PRODUCTION READY
Realtor Dashboard         DONE       DONE       YES        PRODUCTION READY
Client Dashboard          DONE       DONE       YES        PRODUCTION READY
Staff Dashboard           DONE       DONE       YES        PRODUCTION READY
Super-Admin Dashboard     DONE       DONE       YES        PRODUCTION READY
Multi-Tenant (Companies)  DONE       DONE       YES        PRODUCTION READY
Audit Logging             DONE       DONE       YES        PRODUCTION READY
Cron Jobs                 DONE       N/A        YES        PRODUCTION READY
Contact Form              DONE       DONE       YES        PRODUCTION READY
Global Search             DONE       DONE       YES        PRODUCTION READY
Export (CSV/XLSX)         DONE       DONE       YES        PRODUCTION READY
AI Features               DONE       DONE       YES        PRODUCTION READY
2FA (TOTP)                DONE       DONE       YES        PRODUCTION READY
DB Backups (Scripts)      DONE       N/A        N/A        SCRIPTS READY
Monitoring (Sentry)       DONE       N/A        N/A        NEEDS DSN CONFIG
Log Aggregation           DONE       N/A        N/A        NEEDS TOKEN CONFIG
Event Management          DONE       DONE       YES        PRODUCTION READY
Event Registration Email  DONE       N/A        YES        PRODUCTION READY
Event QR Check-in         DONE       DONE       YES        PRODUCTION READY
DB Migrations             DONE       N/A        N/A        BASELINED
Automated Tests           PARTIAL    MINIMAL    N/A        NEEDS EXPANSION
Terms/Privacy Pages       N/A        MISSING    N/A        NOT STARTED
OAuth Frontend Wiring     DONE       PARTIAL    PARTIAL    WIRING NEEDED
Email Verification E2E    PARTIAL    PARTIAL    PARTIAL    NEEDS TESTING

================================================================================
12. RECENT CHANGES (2026-03-14 to 2026-03-23)
================================================================================

----------------------------------------------------------------------
2026-03-14 to 2026-03-16 (Phases 1–16)
----------------------------------------------------------------------

Phase 1  - Database migrations baselined (prisma migrate)
Phase 2  - Vercel cron schedules added to backend/vercel.json
Phase 3  - Contact form endpoint + email (POST /contact, rate limited 3/min)
Phase 4  - Google + Facebook OAuth (Passport strategies, graceful degradation)
Phase 5  - TOTP 2FA (RFC 6238, 8 backup codes, no external lib)
Phase 6  - Global search module (GET /search, role-scoped results)
Phase 7  - Notification preferences (GET/PUT /settings/notifications)
Phase 8  - Export module (sales/tax/payroll/audit/commission → CSV/XLSX)
Phase 9  - AI module (OpenAI + rule-based fallbacks for 4 endpoints)
Phase 10 - Sentry error tracking (common/services/sentry.service.ts)
Phase 11 - DB backup scripts (backend/scripts/backup.sh + restore.sh)
Phase 12 - Per-endpoint rate limiting (login 5/min, register 3/min, etc.)
Phase 13 - CDN image optimization (sharp → WebP, CdnService in UploadModule)
Phase 14 - Structured JSON logging (LoggingInterceptor + Logtail integration)
Phase 15 - Unit test spec files for twofa, search, contact, settings services
Phase 16 - PWA confirmed (frontend/public/sw.js with Workbox, already existed)

----------------------------------------------------------------------
2026-03-17 — Phase 17: Event Management System
----------------------------------------------------------------------

Database:
  - 4 new Prisma models: Event, EventFormField, EventRegistration, EventAnalytics
  - 4 new enums: EventLocationType, EventStatus, RegistrationStatus, FormFieldType
  - Migration: backend/prisma/migrations/20260317000000_add_event_system/

Backend (src/modules/event/):
  - EventModule, EventController, EventService
  - RegistrationService (public registration + QR check-in)
  - QrService (JWT token signing, QR PNG generation)
  - EventAnalyticsService (view + registration + check-in metrics, cached 120s)
  - DTOs: CreateEventDto, UpdateEventDto, CreateRegistrationDto, CheckInDto
  - Unit tests: event.service.spec.ts, registration.service.spec.ts

Frontend:
  - /event/[slug] — public event page (form builder rendered, QR on success)
  - /dashboard/admin/events — list with publish/close/delete
  - /dashboard/admin/events/create — event + custom form builder
  - /dashboard/admin/events/[id] — edit event
  - /dashboard/admin/events/[id]/registrations — manage registrations, CSV export
  - /dashboard/admin/events/[id]/checkin — camera QR scanner + manual entry
  - Admin sidebar: "Events" link added to Content & Comms group
  - Homepage: Upcoming Events section (parallel API fetch with CMS)

----------------------------------------------------------------------
2026-03-18 — Bug Fixes + Registration Email
----------------------------------------------------------------------

Fix: Admin events page crash
  - Root cause: Frontend read res.data.data / res.data.meta.totalPages but
    TransformInterceptor flattens paginated responses to top-level.
  - Fix: Changed to res.data and res.meta?.totalPages.
  - Affected: admin/events/page.tsx, admin/events/[id]/registrations/page.tsx

Fix: QR check-in 400 errors (camera scan path)
  - Root cause: qrService.verifyToken() was throwing on Vercel due to JWT
    secret mismatch between signing time (registration) and verify time
    (check-in) in different serverless invocations.
  - Fix: Removed JWT signature re-verification from check-in; rely on DB
    lookup by qrCodeToken alone (token is unguessable 200+ char string).
  - File: backend/src/modules/event/registration.service.ts

Fix: Manual check-in "Invalid or tampered QR code"
  - Root cause: Check-in endpoint only accepted JWT tokens.
  - Fix: Added regex detection of EVT-... registration codes; those bypass
    JWT path and go straight to findFirst({ registrationCode }).
  - File: backend/src/modules/event/registration.service.ts

Feature: Registration confirmation email
  - New method: MailService.sendEventRegistrationEmail()
  - QR code embedded as CID inline attachment (not data URI, avoids blockers)
  - Sent fire-and-forget after successful registration (won't block response)
  - MailService exported from QueueModule (@Global) for injection everywhere
  - Files: mail.service.ts, queue.module.ts, registration.service.ts

----------------------------------------------------------------------
2026-03-22 — WhatsApp-Level Chat + CRM Intelligence
----------------------------------------------------------------------

DATABASE:
  - New model: VoiceMessage — 1:1 with Message; stores audioUrl (Vercel Blob),
    duration (ms), waveform (Float[], 40 bars)
  - New model: CrmActivity — logs every chat/voice message per client with
    activityType, roomId, messageId, metadata, createdAt
  - Composite index (roomId, createdAt DESC) on messages table — covers the hot
    message-pagination query with single index scan; zero sort step overhead
  - Indexes on chat_rooms.lastMessageAt and chat_rooms.type
  - Migration: backend/prisma/migrations/20260322000000_chat_message_perf_indexes/

BACKEND — Chat Module:
  - ChatController: added POST /rooms/:id/voice (multipart), POST /rooms/:id/typing,
    GET /crm/hot-leads, /crm/cold-leads, /crm/follow-ups, /crm/client/:id,
    /crm/client/:id/activities
  - ChatService.getMessages(): rewritten — separate fast COUNT membership check,
    then plain WHERE roomId=? message query (no participant JOIN per row);
    cursor-based pagination replaces page/offset
  - ChatModule: added MulterModule (memoryStorage), VoiceService, CrmService
  - VoiceService: uploads audio Blob to Vercel Blob; generates 40-bar waveform by
    byte-sampling the audio buffer and normalising amplitudes to 0.10–0.95
  - CrmService: auto-logs CrmActivity on every message send; computes engagement
    score = min(100, recentActivityCount × 5); classifies hot/warm/cold leads;
    follow-up list = clients with no CrmActivity in the last 7 days

BACKEND — API (lib/api.ts):
  - Added postForm<T>(): multipart/form-data POST with no Content-Type header
    (browser sets multipart boundary automatically)

FRONTEND — Components:
  - VoiceRecorder (src/components/chat/voice-recorder.tsx):
      Click-to-start / click-to-stop recording via MediaRecorder API
      Live duration counter displayed as M:SS
      Cancel (X) and Send buttons active during recording
      MIME detection: prefers audio/webm;codecs=opus → ogg → m4a
  - AudioPlayer (src/components/chat/audio-player.tsx):
      40-bar waveform visualization; bar heights driven by amplitude array
      Bars highlight progressively as audio plays (based on currentTime/duration)
      Click anywhere on waveform to seek
      Play/Pause toggle button; elapsed/total time display

FRONTEND — Chat Page:
  - VoiceRecorder placed in input bar; hidden when text is typed
  - Send button shown only when newMessage has content or attachments exist
  - VOICE message type renders AudioPlayer instead of text bubble
  - sendVoiceMessage() destructured from useChat()

FRONTEND — CRM Dashboard:
  - /dashboard/admin/crm (src/app/dashboard/admin/crm/page.tsx):
      Tabs: Hot Leads / Follow-ups / Cold Leads
      Stats row: lead count + average engagement score
      LeadCard with ScoreBar (green ≥ 50, yellow 21–49, red ≤ 20)
  - Sidebar: "CRM" nav item added to Sales & Finance group (Target icon)

----------------------------------------------------------------------
2026-03-23 — System-Wide Speed Optimisations
----------------------------------------------------------------------

BACKEND:
  - main.ts: removed !process.env.VERCEL guard from compression() — Vercel CDN
    does NOT compress serverless JSON responses automatically; enabling gzip
    unconditionally cuts all API response sizes by 60–80%
  - LoggingInterceptor: added SKIP_PATHS = ['/health']; keepalive pings now
    bypass all logging overhead with an immediate next.handle() passthrough

FRONTEND:
  - authenticated-providers.tsx: added KeepAlive component that pings
    GET /health every 4 min (first ping at 60 s after mount); prevents Vercel
    cold starts (idle functions go cold after ~5 min, costing 2–5 s per user)
  - chat-storage.ts (new file): localStorage persistence utility:
      Schema-versioned (v=2), keyed by userId
      Stores up to 20 rooms × 30 messages each
      loadChatStorage() / saveChatStorage() with graceful quota-exceeded handling
  - chat-context.tsx: major rewrite for WhatsApp-like instant load:
      Mount effect hydrates state from localStorage before any network request
      Background fetchRooms() called immediately after hydration (non-blocking)
      30 s cache TTL — selectRoom() skips network fetch when cache is fresh
      roomsRef always-current ref prevents stale closure bugs in callbacks
      persist() helper called after every send/receive/fetch event
      subscribeToTyping() extracted as standalone helper (no duplication)
      Real-time message handler saves to localStorage on every new message

================================================================================
LEGEND:
  DONE           = Fully implemented and functional
  PARTIAL        = Some implementation, needs completion
  MISSING        = Not implemented at all
  UI ONLY        = Frontend UI exists but no backend support
  NEEDS TESTING  = Implemented but not fully verified
  NEEDS WORK     = Requires significant additional development
  NEEDS CONFIG   = Implemented but requires env var to activate
  NOT STARTED    = No work done yet
================================================================================

END OF DOCUMENTATION
